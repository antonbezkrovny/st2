---
name: "chatops.hetzner_get_servers_info"
action_ref: "hetzner.getServersInfo"
description: "Get Hetzner VM info"
formats:
  - display: "ansible-playbook"
    representation:
      - "ansible-playbook -c local /opt/stackstorm/packs/st2/playbooks/hetzner_get_servers_info.yml -i localhost,"
result:
  format: |
    Ansible command result: {~}
    {% if execution.result.stderr %}*Stdout:* {% endif %}
    ```{{ execution.result.stdout }}```
    {% if execution.result.stderr %}*Stderr:* ```{{ execution.result.stderr }}```{% endif %}
  extra:
    slack:
      color: "{% if execution.result.succeeded %}good{% else %}danger{% endif %}"
